#include<stdio.h>
#include<stdlib.h>
#include "graph.h"

void createNodes(int, node**);
void printGraph_cmd(node*);
pNode GetNode(int data, node**);
void createGraph(node**);
void remove_node(node**, int);
void deleteGraph_cmd();
